include "system.cp";

class Test {
  val x : int = 10;
  var y : int = 0;

  def test(x : int, y : int) {
  
  }

  def xxx(x : boolean) {
    var x2 : int;
    var x1 : int = 10 * 20 + x2;
    x1 = 20;

    val y : boolean = true;

    if (y && x1 < x2 + 10) {
      val m1 : int = 10 + 20;
    } else {
      val m1 : int = 5;
      
    }

  }
}

